{
  "name": "gerador-ambulatorial",
  "version": "0.1.0",
  "private": true,
  "description": "Aplicativo Electron local para geração de lotes ambulatoriais em PDF.",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "googleapis": "^173.0.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "electron": "^42.4.1"
  },
  "engines": {
    "node": ">=20"
  }
}
